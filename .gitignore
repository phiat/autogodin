# Local-only collaboration / tooling
AGENTS.md
CLAUDE.md
.claude/
.beads/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Local env (per-machine paths, secrets); .env.example is committed
.env

# Upstream autogo sibling clone (kept external; pinned via autogo.pin,
# fetched via scripts/setup_autogo.sh — see 3xv.13).
autogo/

# Odin build artifacts
build/
*.so
*.dll
*.dylib
*.o
*.a
# Compiled experiment binaries (built next to their bench.odin source)
experiments/*/bench

# Python build / cache
__pycache__/
*.pyc
*.egg-info/

# Editor / OS
.DS_Store
*.swp
game_data/

# Postmortem checkpoint (46 MB, local-only)
experiments/2026-05-17_13-47-bpoC-katago-seed/postmortem/iter0_best.pt

experiments/2026-05-17_16-00-bpoC-rerun-cpp/postmortem/iter4_best.pt
